#ifndef CHANNEL_HPP
#define CHANNEL_HPP

// Channel behavior will be added with the channel commands.  The definition is
// kept here so Server can own its channel map while registration is developed.
class Channel
{
};

#endif
